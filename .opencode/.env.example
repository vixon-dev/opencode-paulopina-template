# OpenCode project template env (SAFE TO COMMIT)
# Copy to `.opencode/.env` and fill secrets:
#   cp .opencode/.env.example .opencode/.env
#   chmod 600 .opencode/.env

# n8n MCP
N8N_API_URL=https://api.multiagents.pro
N8N_API_KEY=

# Context7 MCP
CONTEXT7_API_KEY=

# Postgres MCP (connection string only; no Supabase API dependency)
# Example: postgresql://user:pass@host:5432/db?sslmode=require
PG_MODE=readonly
PG_URL=

# Redis MCP (optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Force MCP logs into project folder
MCP_LOG_DIR="$OPENCODE_PROJECT_ROOT/.opencode/logs"

# Safety flags (keep false by default)
REDIS_ALLOW_INSERT=false
REDIS_ALLOW_UPDATE=false
REDIS_ALLOW_DELETE=false
REDIS_ALLOW_CREATE=false
REDIS_ALLOW_DROP=false

# OpenAPI MCP
OPENAPI_SPECS_DIR="$OPENCODE_PROJECT_ROOT/.opencode/openapi"

# ntfy.sh notifications (optional)
# Used by .opencode/notify.sh.
# Example topic: opencode-myproject
NTFY_TOPIC=opencode-myproject
